---
extensions:
  - name: "Legacy file extension for YAML"
    description: "The .yml file extension is an older alternative for YAML files."
    categories:
    - data-format
    alternative: yaml
---

# Alternative extension for YAML

The `.yml` extension is an older, shorter alternative to `.yaml`. Modern tooling and communities generally recommend using `.yaml` for clarity. 

Wikipedia: [YAML](https://en.wikipedia.org/wiki/YAML)

